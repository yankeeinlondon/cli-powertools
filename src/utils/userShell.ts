import { Shell } from "~/types";

/**
 * Detects the user's default interactive shell program.
 * 
 * - `bash`, `zsh`, etc. for **macOS** and **Linux**
 * - `powershell`, ``, etc. for **Windows**
 */
export function userShell(): Shell {
    // TODO
}

